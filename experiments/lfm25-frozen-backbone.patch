diff --git a/experiments/eval_combined_generic_head.py b/experiments/eval_combined_generic_head.py
index f21b3c5..7b7909d 100644
--- a/experiments/eval_combined_generic_head.py
+++ b/experiments/eval_combined_generic_head.py
@@ -26 +25,0 @@ from prepare_combined_generic import (
-    PROMPTSHIELD_TEST,
@@ -28,2 +26,0 @@ from prepare_combined_generic import (
-    REPO_ROOT,
-    SEP,
@@ -36 +33 @@ from prepare_combined_generic import (
-from prepare_full_combined_generic import PAIR_ARCHIVE, PAIR_ARCHIVE_SHA256
+from prepare_full_combined_generic import PAIR_ARCHIVE_SHA256
@@ -88,0 +86,7 @@ from morgott.detector import choose_threshold
+SOURCE_ROOT = Path(__file__).resolve().parent.parent
+REPO_ROOT = Path(os.environ.get("MORGOTT_REPO_ROOT", SOURCE_ROOT)).resolve()
+PROMPTSHIELD_TEST = (
+    REPO_ROOT / "artifacts/external_eval_data/promptshield/test.jsonl"
+)
+SEP = REPO_ROOT / "artifacts/external_eval_data/sep/sep.jsonl"
+PAIR_ARCHIVE = REPO_ROOT / "artifacts/matched_pairs/pairs_20260726T105034Z.jsonl"
@@ -124 +128 @@ def _evaluator_source_paths(
-            REPO_ROOT / "experiments/prepare_combined_generic.py"
+            SOURCE_ROOT / "experiments/prepare_combined_generic.py"
@@ -127 +131 @@ def _evaluator_source_paths(
-            REPO_ROOT / "experiments/prepare_full_combined_generic.py"
+            SOURCE_ROOT / "experiments/prepare_full_combined_generic.py"
@@ -130 +134 @@ def _evaluator_source_paths(
-            REPO_ROOT / "experiments/train_combined_generic_head.py"
+            SOURCE_ROOT / "experiments/train_combined_generic_head.py"
@@ -132,3 +136,3 @@ def _evaluator_source_paths(
-        "strict_normalizer": REPO_ROOT / "experiments/strict_normalize.py",
-        "descriptive_threshold_helper": REPO_ROOT / "src/morgott/detector.py",
-        "canonical_text_helper": REPO_ROOT / "src/morgott/data.py",
+        "strict_normalizer": SOURCE_ROOT / "experiments/strict_normalize.py",
+        "descriptive_threshold_helper": SOURCE_ROOT / "src/morgott/detector.py",
+        "canonical_text_helper": SOURCE_ROOT / "src/morgott/data.py",
@@ -138 +142 @@ def _evaluator_source_paths(
-            REPO_ROOT / "experiments/train_full_combined_generic_head.py"
+            SOURCE_ROOT / "experiments/train_full_combined_generic_head.py"
@@ -142 +146 @@ def _evaluator_source_paths(
-            REPO_ROOT / "experiments/train_combined_generic_lora.py"
+            SOURCE_ROOT / "experiments/train_combined_generic_lora.py"
@@ -1066 +1070 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "experiments/prepare_full_combined_generic.py",
+                SOURCE_ROOT / "experiments/prepare_full_combined_generic.py",
@@ -1070 +1074 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "experiments/prepare_combined_generic.py",
+                SOURCE_ROOT / "experiments/prepare_combined_generic.py",
@@ -1074 +1078 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "experiments/strict_normalize.py",
+                SOURCE_ROOT / "experiments/strict_normalize.py",
@@ -1078 +1082 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "src/morgott/overlap.py",
+                SOURCE_ROOT / "src/morgott/overlap.py",
@@ -1082 +1086 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "src/morgott/data.py",
+                SOURCE_ROOT / "src/morgott/data.py",
@@ -1089 +1093 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "experiments/prepare_combined_generic.py",
+                SOURCE_ROOT / "experiments/prepare_combined_generic.py",
@@ -1093 +1097 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "experiments/strict_normalize.py",
+                SOURCE_ROOT / "experiments/strict_normalize.py",
@@ -1097 +1101 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "src/morgott/overlap.py",
+                SOURCE_ROOT / "src/morgott/overlap.py",
@@ -1101 +1105 @@ def _verify_selection_report(report: dict, *, full: bool) -> None:
-                REPO_ROOT / "src/morgott/data.py",
+                SOURCE_ROOT / "src/morgott/data.py",
@@ -1316 +1320 @@ def discover_run(run_directory: Path) -> dict:
-        trainer_path = REPO_ROOT / "experiments/train_full_combined_generic_head.py"
+        trainer_path = SOURCE_ROOT / "experiments/train_full_combined_generic_head.py"
@@ -1319 +1323 @@ def discover_run(run_directory: Path) -> dict:
-            REPO_ROOT / "experiments/train_combined_generic_head.py",
+            SOURCE_ROOT / "experiments/train_combined_generic_head.py",
@@ -1324 +1328 @@ def discover_run(run_directory: Path) -> dict:
-            REPO_ROOT / "experiments/prepare_combined_generic.py",
+            SOURCE_ROOT / "experiments/prepare_combined_generic.py",
@@ -1330 +1334 @@ def discover_run(run_directory: Path) -> dict:
-        trainer_path = REPO_ROOT / (
+        trainer_path = SOURCE_ROOT / (
@@ -1338 +1342 @@ def discover_run(run_directory: Path) -> dict:
-                REPO_ROOT / "experiments/train_combined_generic_head.py",
+                SOURCE_ROOT / "experiments/train_combined_generic_head.py",
@@ -1370 +1374 @@ def discover_run(run_directory: Path) -> dict:
-        REPO_ROOT / "experiments/strict_normalize.py",
+        SOURCE_ROOT / "experiments/strict_normalize.py",
@@ -1664 +1668 @@ def _load_model(run: dict):
-    from transformers import AutoModel, AutoTokenizer
+    from transformers import AutoModel, AutoModelForMaskedLM, AutoTokenizer
@@ -1666,0 +1671 @@ def _load_model(run: dict):
+    is_lfm = result["model_id"] == "LiquidAI/LFM2.5-Encoder-230M"
@@ -1669,0 +1675 @@ def _load_model(run: dict):
+        trust_remote_code=is_lfm,
@@ -1673,6 +1679,17 @@ def _load_model(run: dict):
-    encoder = AutoModel.from_pretrained(
-        result["model_id"],
-        revision=result["model_revision"],
-        attn_implementation=result["attention_implementation"],
-        dtype=torch.bfloat16,
-    )
+    if is_lfm:
+        model = AutoModelForMaskedLM.from_pretrained(
+            result["model_id"],
+            revision=result["model_revision"],
+            attn_implementation=result["attention_implementation"],
+            dtype=torch.bfloat16,
+            trust_remote_code=True,
+        )
+        encoder = model.lfm2
+        del model
+    else:
+        encoder = AutoModel.from_pretrained(
+            result["model_id"],
+            revision=result["model_revision"],
+            attn_implementation=result["attention_implementation"],
+            dtype=torch.bfloat16,
+        )
diff --git a/experiments/train_combined_generic_head.py b/experiments/train_combined_generic_head.py
index 13c8ffa..b6e77e1 100644
--- a/experiments/train_combined_generic_head.py
+++ b/experiments/train_combined_generic_head.py
@@ -29 +29,2 @@ from strict_normalize import strict_normalize
-REPO_ROOT = Path(__file__).resolve().parent.parent
+SOURCE_ROOT = Path(__file__).resolve().parent.parent
+REPO_ROOT = Path(os.environ.get("MORGOTT_REPO_ROOT", SOURCE_ROOT)).resolve()
@@ -706 +707 @@ def validate_selection_report(
-            REPO_ROOT
+            SOURCE_ROOT
@@ -714 +715 @@ def validate_selection_report(
-            REPO_ROOT / "experiments/strict_normalize.py"
+            SOURCE_ROOT / "experiments/strict_normalize.py"
@@ -716,2 +717,2 @@ def validate_selection_report(
-        "overlap_module_sha256": file_sha256(REPO_ROOT / "src/morgott/overlap.py"),
-        "canonical_text_helper_sha256": file_sha256(REPO_ROOT / "src/morgott/data.py"),
+        "overlap_module_sha256": file_sha256(SOURCE_ROOT / "src/morgott/overlap.py"),
+        "canonical_text_helper_sha256": file_sha256(SOURCE_ROOT / "src/morgott/data.py"),
@@ -723 +724 @@ def validate_selection_report(
-                    REPO_ROOT / "experiments/prepare_combined_generic.py"
+                    SOURCE_ROOT / "experiments/prepare_combined_generic.py"
diff --git a/experiments/train_full_combined_generic_head.py b/experiments/train_full_combined_generic_head.py
index 469306d..62a86ce 100644
--- a/experiments/train_full_combined_generic_head.py
+++ b/experiments/train_full_combined_generic_head.py
@@ -32 +32 @@ import numpy as np
-from prepare_combined_generic import REPO_ROOT, TARGET, canonical_record, file_sha256
+from prepare_combined_generic import TARGET, canonical_record, file_sha256
@@ -47,0 +48,2 @@ from train_combined_generic_head import (
+SOURCE_ROOT = Path(__file__).resolve().parent.parent
+REPO_ROOT = Path(os.environ.get("MORGOTT_REPO_ROOT", SOURCE_ROOT)).resolve()
@@ -1223,3 +1225,4 @@ def main() -> int:
-    head_helper_path = REPO_ROOT / "experiments/train_combined_generic_head.py"
-    strict_normalizer_path = REPO_ROOT / "experiments/strict_normalize.py"
-    canonical_projection_path = REPO_ROOT / "experiments/prepare_combined_generic.py"
+    source_root = Path(__file__).resolve().parent.parent
+    head_helper_path = source_root / "experiments/train_combined_generic_head.py"
+    strict_normalizer_path = source_root / "experiments/strict_normalize.py"
+    canonical_projection_path = source_root / "experiments/prepare_combined_generic.py"
@@ -1268 +1271 @@ def main() -> int:
-    from transformers import AutoModel, AutoTokenizer
+    from transformers import AutoModelForMaskedLM, AutoTokenizer
@@ -1271 +1274,5 @@ def main() -> int:
-    tokenizer = AutoTokenizer.from_pretrained(args.model_id, revision=revision)
+    tokenizer = AutoTokenizer.from_pretrained(
+        args.model_id,
+        revision=revision,
+        trust_remote_code=True,
+    )
@@ -1274 +1281 @@ def main() -> int:
-    encoder = AutoModel.from_pretrained(
+    model = AutoModelForMaskedLM.from_pretrained(
@@ -1278,0 +1286 @@ def main() -> int:
+        trust_remote_code=True,
@@ -1279,0 +1288,2 @@ def main() -> int:
+    encoder = model.lfm2
+    del model
